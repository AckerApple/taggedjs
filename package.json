{
  "name": "taggedjs",
  "private": true,
  "version": "2.8.21",
  "description": "tagged template reactive html",
  "main": "js/index.js",
  "type": "module",
  "scripts": {
    "build:js": "rm -rf js && mkdir js && tsc -p tsconfig.build.json",
    "build": "npm-run-all --sequential build:js build:webpack build:dist:only && echo \"✅ Build completed\"",
    "build:others": "npm-run-all --sequential build:gh-pages build:pinbowl",
    "build:webpack": "npx webpack",
    "build:gh-pages": "cd ../gh-pages && npm install ../main/dist --no-save && npm run build && echo \"🔬 Built gh-pages\"",
    "build:gh-pages:old": "npm run build:copy:gh-pages && cd ../gh-pages && npm run build && echo \"🔬 Built gh-pages\"",
    "build:copy:gh-pages": "cp -R dist/. ../gh-pages/node_modules/taggedjs",
    "build:watch:gh-pages": "chokidar 'ts/**/*' -c 'npm run build:js && npm run build:dist:only && npm run build:gh-pages'",
    "build:dist": "run-s build:js build:webpack build:dist:only",
    "build:dist:only": "cp README.MD ./dist/README.md && npm run build:dist:pack && rm -rf ./dist/js && cp -R js ./dist && cp -R ./dist/. ../dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "jest",
    "save:dist": "run-s patch:version build:dist save:dist:git",
    "save:dist:git": "cd ../dist && git add . && git commit -m \"dist patch\" && git push",
    "save": "npm run lint:fix && git add . && git commit -m \"patch\" && git push",
    "build:dist:pack": "cp package.json ./dist/package.json && echo \"✅ copied package.json to ./dist\" && node scripts/package-dist.cjs",
    "patch:version": "npm --no-git-tag-version version patch",
    "publish": "npm run test && npm run build:dist && npm run save:dist && npm run save && cd ../dist && npm publish",
    "build:watch": "chokidar 'ts/**/*' -c 'npm run build'",
    "build:pinbowl": "cp -R dist/. ../../../services/pinbowl/src/taggedjs && cd ../../../services/pinbowl/ && npm run build && echo \"🎳 Built pinbowl\"",
    "build:watch:pinbowl": "chokidar 'ts/**/*' -c 'npm run build:js && npm run build:webpack && npm run build:dist:only && npm run build:pinbowl'",
    "build:dump:gh-pages": "cp -R js/. ../../taggedjs-dump/gh-pages/node_modules/taggedjs/js && cd ../../taggedjs-dump/gh-pages/ && npm run build && echo \"🔭 Built dump#gh-pages\"",
    "build:watch:dump:gh-pages": "chokidar 'ts/**/*' -c 'npm run build:js && npm run build:dist:only && npm run build:dump:gh-pages'"
  },
  "keywords": [
    "react",
    "angular",
    "vue",
    "svelte",
    "alternative",
    "works",
    "native",
    "in",
    "web",
    "browser"
  ],
  "author": "Acker Dawn Apple",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.23.6",
    "@babel/preset-env": "^7.23.6",
    "@eslint/js": "^9.10.0",
    "@types/jest": "^29.5.12",
    "chokidar-cli": "^3.0.0",
    "compression-webpack-plugin": "^11.1.0",
    "eslint": "^9.10.0",
    "globals": "^15.9.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "npm-run-all": "^4.1.5",
    "terser-webpack-plugin": "^5.3.10",
    "ts-jest": "^29.1.2",
    "ts-loader": "^9.5.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.6.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
