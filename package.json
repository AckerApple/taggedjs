{
  "name": "taggedjs",
  "version": "0.0.10",
  "description": "tagged template reactive html",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "npx webpack",
    "build:dist": "npm run build && cp README.MD dist/README.md && npm run build:dist:pack && cp -R dist/. ../dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "save:dist": "npm run patch:version && npm run build:dist && git add . && git commit -m \"dist patch\" && git push",
    "save": "git add . && git commit -m \"patch\" && git push",
    "build:dist:pack": "cp package.json dist/package.json && node scripts/package-dist.cjs",
    "patch:version": "npm --no-git-tag-version version patch",
    "publish": "npm run build:dist && npm run save:dist && cd ../dist && npm publish"
  },
  "keywords": [
    "react",
    "angular",
    "vue",
    "svelte",
    "alternative",
    "works",
    "native",
    "in",
    "web",
    "browser"
  ],
  "author": "Acker Dawn Apple",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.23.6",
    "@babel/preset-env": "^7.23.6",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
