import { ValueSubjective } from "taggedjs";
/** this tag renders only once */
export declare const oneRender: () => (counter?: ValueSubjective<number>, renderCount?: number) => import("taggedjs").Tag;
