export declare const arrayScoreData: import("taggedjs").TaggedFunction<({ score, playerIndex }: {
    playerIndex: number;
    score: {
        score: number;
        frame: number;
    };
}) => import("taggedjs").Tag>;
